Right-Click on start_opencv_install.bat script and run as Administrator

After that follow the prompts.

-- You can enter a specific version of opencv
-- You can enter a specific install directory (enter a "prefix")
-- You can enter a specific location of where the opencv_contrib otherwise the version will be the latest or match the opencv version entered


Should download the git repo for both opencv and opencv_contrib then attempt to build and install.